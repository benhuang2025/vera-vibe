in my understanding, the popular development workflow is SDD, and the tool we are using now is OpenSpec + Claude UI+MiniMax2.5.
this prototyping will not involve multi-agents to do separate works and integrate. what we need to do is to define and iterate the sepc, which will give the AI agent instructions exactly what do do.

我明确一下 本次tournament，限于我们ZKVM目前的本身实现不具备真的隐私属性，不要求真的隐私性，只是可以做成把具体的操作参数做成private input，表达这么一个一个意思